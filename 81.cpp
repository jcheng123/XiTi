#include <iostream>
#include <vector>

using namespace std;

bool search(vector<int>& nums, int target)
{
	int s = 0, e = nums.size() - 1; // [s, e] 
	int mid = s + (e - s)/2;

	while(s < e)
	{
		if(nums[mid] >= nums[0] )
	}


	while(target > nums[mid])
	{

	}
}